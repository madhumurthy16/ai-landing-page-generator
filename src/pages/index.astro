---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
---

<Layout>
	<div class="app-header">
		<h3 class="heading-three app-header-logo">PageCraftAI</h3>
		<p class="font-xsmall app-header-badge">Free tier — 20 pages/month</p>
	</div>
	<Hero
		headline="Build Stunning Landing Pages in Seconds"
		subheadline="Describe your product, choose your tone, and let AI generate a complete, professional landing page — no design skills needed."
		ctaLabel="Try It Free"
		ctaHref="#generate"
	/>
	<div class="app-container" id="generate">

		<div class="app-body">
			<section class="form-panel">
				<div class="form-panel-header">
					<h2 class="heading-two form-panel-title">Describe your product</h2>
					<p class="body-large form-panel-subtitle">Fill in the fields below and we'll generate a complete, professional landing page in seconds.</p>
				</div>
				<div class="form-divider"></div>
				<form>
					<div class="form-group">
						<label for="product-name" class="font-small field-label">Product Name<span class="font-xsmall required-field">Required</span></label>
						<input id="product-name" class="font-medium" placeholder = "What is the name of your product" type="text" />
					</div>
					<div class="form-group">
						<label for="product-desc" class="font-small field-label">Product Description <span class="font-xs required-field">Required</span></label>
						<textarea id="product-desc" class="font-medium" placeholder="Provide a one sentence description of the product" />
					</div>
					<div class="form-group">
						<label for="target-audience" class="font-small field-label">Target Audience <span class="font-xsmall required-field">Required</span></label>
						<input id="target-audience"  class="font-medium" placeholder="Who is your target audience?" type="text" />
					</div>
					<div class="form-group">
						<fieldset>
							<span class="font-xsmall required-field">Required</span>
							<legend class="font-small field-label">Key Benefits</legend>
							<div class="benefits-row">
								<div class="benefits-row-one">
									<label  for="key-benefits-one" class="font-small visually-hidden">Key Benefit 1</label>
									<input id="key-benefits-one" class="font-medium" placeholder="Key Benefit 1" />
								</div>
								<div class="benefits-row-two">
									<label  for="key-benefits-two" class="font-small visually-hidden">Key Benefit 2</label>
									<input id="key-benefits-two" class="font-medium" placeholder="Key Benefit 2" />
									
								</div>
								<div class="benefits-row-three">
									<label  for="key-benefits-three" class="font-small visually-hidden">Key Benefit 3</label>
									<input id="key-benefits-three" class="font-medium" placeholder="Key Benefit 3" />
								</div>
							</div>
						</fieldset>
					</div>  

					<div class="form-group">
						<label for="tone-of-voice" class="font-small field-label">Tone of Voice</label>
						<select class="form-select font-medium">
							<option selected>Professional</option>
							<option>Friendly & Conversational</option>
							<option>Bold & Urgent</option>
							<option>Playful & Witty</option>
							<option>Minimalist & Premium</option>
						</select>
					</div>
					
					<input class="btn-primary font-btn" type="submit" value="Generate My Landing Page" />
					
					<div class="btn-secondary-row">
						<button class="btn-success btn-secondary font-secondary-btn">Copy HTML File</button>
						<button class="btn-download btn-secondary font-secondary-btn">Download HTML File</button>
					</div>
				</form>
			</section>
			
			<section class="preview-panel">
				<p>Preview appears here after generating.</p>
				<iframe id="previewFrame" class="preview-frame"></iframe>
			</section>
		</div>
	</div>

</Layout>

<style>
	.app-container {
		margin: 2rem 4rem 4rem;
		background: #FFFFFF;
		border: 1.5px solid var(--gray-200);
		border-radius: 10px;
		overflow: hidden;
	}

	.app-header {
		background: linear-gradient(135deg, var(--primary-indigo-900) 0%, var(--primary--indigo-700) 100%);
		padding: 14px 32px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-bottom: 1px solid rgba(255, 255, 255, 0.08);
	}

	.app-header-logo {
		color: #FFFFFF;
	}

	.app-header-badge {
		display: none;
		align-items: center;
		color: var(--primary--indigo-100);
		background: rgba(255, 255, 255, 0.1);
		border: 1px solid rgba(255, 255, 255, 0.18);
		border-radius: 999px;
		padding: 3px 12px;
	}

	.app-body {
		display: grid;
		grid-template-columns: 1fr;

	}

	.form-panel {
		padding: 16px 18px;
		margin-bottom: 16px;

	}

	.form-panel-title {
		margin-bottom: 16px;

	}

	.form-panel-subtitle {
		margin-bottom: 16px;
	}

	.required-field {
		display: none;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		margin-bottom: 16px;
	}

	.field-label {
		margin-bottom: 8px;
	}

	input,
	textarea,
	select {
		border: 1.5px solid var(--gray-300);
		border-radius: 7px;
		background-color: #F9FAFB;
		padding: 0 10px;
		color: var(--gray-900);
		outline: none;
		transition: border-color 0.3s ease;
	}

	input:focus,
	textarea:focus,
	select:focus {
		border: 1.5px solid var(--primary-indigo-900);
	}

	input, 
	select {
		height: 36px;
	}

	textarea {
		height: 50px;
	}

	fieldset {
		border: 0;
	}

	.visually-hidden {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	#key-benefits-one,
	#key-benefits-two,
	#key-benefits-three {
		width: 100%;
	}

	#key-benefits-one,
	#key-benefits-two {
		margin-bottom: 8px;
	}

	.btn-primary {
		width: 100%;
		height: 40px;
		background-color: var(--accent-rose-500);
		color: #FFFFFF;
		border-radius: 8px;
		margin-bottom: 8px;
		border: 0;
	}

	.btn-secondary-row {
		display: flex;
		flex-direction: column;
	}

	.btn-success {
		margin-bottom: 8px;
	}

	.btn-download {
		margin-bottom: 16px;
	}

	.btn-secondary {
		height: 40px;
		border: 1.5px solid var(--primary-indigo-900);
		background-color: #FFFFFF;
		color: var(--primary-indigo-900);
		border-radius: 8px;
    	cursor: pointer;
	}

	.form-panel {
	}

	.preview-panel {
		background-color: var(--gray-50);
		border-top: 1.5px solid var(--gray-200);
		padding: 16px;
		min-height: 180px;
	}

	.preview-panel p {
		text-align: center;
	}
	
	iframe {
		border: 0;
	}
	
	/* Media Queries */

	@media screen and (min-width: 768px) {
		.app-header {
			justify-content: space-between;
		}

		.app-header-badge {
			display: inline-flex;
		}

		.app-body {
			grid-template-columns: 1fr 50%;
			box-shadow: 0 2px 16px rgba(15, 27, 76, .07);
			border-radius: 14px;
			overflow: hidden;

		}

		.form-panel {
			padding: 40px;
		}

		.preview-panel p {
			margin-top: 40px;
		}
	}

</style>

